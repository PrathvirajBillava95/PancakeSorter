#ifndef __HELPERS_H__
#define __HELPERS_H__

//Helper functions
string flip(string str, int i);	   	// Flips the order of pancakes i times
string CreateOutputString(int n);	// Creates the output order of pancakes

#endif
